import QtQuick 2.15
import QtQuick.Controls 2.15

ComboBox {
    leftPadding: 10
    rightPadding: 28
    topPadding: 4
    bottomPadding: 4
    implicitHeight: Math.max(32, Math.round(font.pixelSize * 2.1))
}
