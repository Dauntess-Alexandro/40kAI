# 40kAI dependencies (Windows, Python 3.12+)
# Версии не зафиксированы — при установке берутся последние релизы с PyPI.
#
# Ручная установка / обновление:
#   1) python -m venv .venv
#   2) .\.venv\Scripts\activate
#   3) python -m pip install -U pip
#   4) python -m pip install -U -r requirements_windows.txt
#
# Установщик Windows: scripts\build_installer.bat -> installer\output\Install.exe
# Установка: Install.exe (корень репо) или installer\install_deps.bat
#   — без аргументов: авто GPU/CUDA + меню выбора (Enter = авто)
#   — install_deps.bat -y     — авто без меню
#   — install_deps.bat cpu    — только CPU
#   — install_deps.bat cu128  — принудительно CUDA 12.8
#
# Это единственный источник правды по Python-зависимостям в репозитории.

gym
gymnasium
matplotlib
numpy
imageio
tqdm
pandas
scipy
torch
PySide6
# torch ставится через install_deps.bat (CPU или CUDA); вручную: pip install torch
numba
pyzmq
msgpack
psutil
nvidia-ml-py
tensorboard
# Dev: линтер/форматтер (используется хуком .claude/hooks/ruff_fix.py)
ruff
